# Deployment 1.7.53, 2026-09-16

The pending-send notice gains a send glyph that skips the rest of the countdown,
and the countdown itself drops from five seconds to three, held once in
`PiedWebUx.sendDelaySeconds`. Six plugin files carry the change —
`background-send.js`, `send-delay.js`, `ux.css`, `composer-icons.js`,
`composer-icons.json` and `index.php`. No theme file changed; the stylesheet
rebuilt to the same bytes.

## Preflight

Read-only comparison of the installed payload against the 1.7.52 manifest: 40 of
40 tracked files identical, none missing, no foreign file, mode 644 throughout —
36 plugin files and 4 theme files. The installed `index.php` announced
`VERSION = '1.7.52'`. Production was a clean 1.7.52 with no drift.

Backups outside the web root, before any write, both directories in full:

- `~/nextsnapmail-pied-web-backups/pied-web-ux-1.7.52-20260916T142221Z/`
  (36 files; `index.php` 6 101 B, SHA-256 `a2d520e1…`)
- `~/nextsnapmail-pied-web-backups/PiedWeb-theme-1.7.52-20260916T142221Z/`
  (4 files; `style.css` 225 832 B, SHA-256 `11fae7e3…`)

Both hashes equal the 1.7.52 manifest, so the backup is the release it claims.

## Installation

The six files were staged privately in `~/.pw-stage-1.7.53` (mode 700), their
bytes compared with the 1.7.53 manifest — all six equal — and the server's own
`php -l` accepted the new `index.php` before any write. The five assets were
installed first and `index.php`, which announces the version, last.

Installed hashes equal the manifest across all 40 payload files, with no drift,
nothing missing, no foreign file and mode 644 throughout. The staging directory
and the two temporary helper scripts were removed.

Workers were recycled with `pkill -u robindfr lsphp` (10 workers before, 0
after) followed by warm-up requests, `status.php` 200 in 0.74 s then 0.06 s. The
served plugin bundle key changed from `61070cefa1e48e4ee8d90d5f2d51b773` to
`c4e5134a5b2a8da36ee3abe56ee2dcb9`, which with `opcache.validate_timestamps=0`
remains the only reliable proof of web activation.

## Verification

- `python3 tools/verify.py`: 40 payload files, reproducible theme, syntax passed.
  Python diagnostics: 5 tests. Browser fixtures for this change:
  `test-send-now.js` 14 checks, `image-markdown-regression.js` 25 checks — all
  passing.
- Authenticated production page, `nc.robin-d.fr/apps/nextsnapmail/`, after the
  recycle: `PiedWebUx.sendDelaySeconds` is `3`, the icon set holds 23 glyphs
  including `send-horizontal`, `createSendDelay` is present and the Pied Web
  theme is active.
- The served bundle itself was read back: it contains `function sendNow`, reads
  `sendDelaySeconds`, and its notice markup carries the `pw-outgoing-now` button.
  The served stylesheet carries the matching rules.
- Drawn from that served markup in the live page and then removed: the control
  measures 44 × 44 with a 20 px glyph, resting background transparent, border
  `0px` — so the `!important` Nextcloud button border is gone in production too —
  and the Undo label reads `Annuler (3)`.
- `scripts/audit-live.py` in the parent notebook, after the write: 59 of 59
  tracked files match the published manifests, Calendar description patch
  present, `.htaccess` directives and the Mail attachment cache window as
  expected (`evidence/live-2026-09-16-mail-1.7.53.json`).
- Nothing was composed, sent, opened, moved, deleted, flagged or marked unread.

## Boundaries

No message was sent, so the phase sequence the glyph triggers — cancel the
delay, mark the job ready, wait for the draft save, then `pump()` — was exercised
against the injected fixture clock and read in the served source, not through a
real submission on production. The live check measured the notice as presentation
only.

The account used for verification renders the light theme, so the dark surfaces
were not measured on production; the fixture's dark pass compares the glyph with
the subject line's token rather than an absolute ratio, because its root is
pinned to `data-themes="light"`.

Narrow-window layout was checked at 390 px in the fixture, not on the production
page.

## Rollback

This release changes six files and adds none:

- copy `~/nextsnapmail-pied-web-backups/pied-web-ux-1.7.52-20260916T142221Z/`
  `background-send.js`, `send-delay.js`, `ux.css`, `composer-icons.js`,
  `composer-icons.json` and `index.php` over the installed plugin files,

then recycle with `pkill -u robindfr lsphp` and a warm-up request and confirm the
bundle key returns to `61070cefa1e48e4ee8d90d5f2d51b773`. The theme backup from
the same timestamp restores the stylesheet, which this release did not change.
The 1.7.52 fingerprints are in this repository's git history. This release writes
no data and no configuration.
