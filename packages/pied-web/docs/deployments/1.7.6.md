# Deployment 1.7.6, 2026-09-13

The reader's **Copier en MD** button now briefly displays a contrasting checkmark
after a successful copy and a distinct cross after failure. The n0c Nextcloud
instance runs Nextcloud 34.0.4.1 (display version 34.0.4), NextSnapMail 0.1.11
and embedded SnappyMail 2.38.2. All 34 previous 1.7.5 payload fingerprints
matched before installation. Only `studio.js`, compiled PiedWeb `style.css` and
version-bearing `index.php` changed.

The three original files were backed up outside the web root at
`~/nextsnapmail-backups/pied-web-ux-v1.7.6-20260913T141241Z/`. The staged files
and release manifest are at
`~/nextsnapmail-staging/pied-web-ux-1.7.6-20260913T141241Z/`. JavaScript and
theme CSS were installed before `index.php`. All 39 installation checks passed.
The actual server PHP runtime compiled the new feedback logic and theme CSS;
CLI plugin hash: `d8469b2143fd2b109118270e1b6ccf2c`. The configuration
fingerprint was unchanged during compilation.

LiteSpeed initially served cached plugin PHP 1.7.5. The guarded authenticated
maintenance hook invalidated the fixed plugin PHP paths; a subsequent Web
request reported 1.7.6. The temporary hook was removed and its configuration
restored byte-for-byte. After Mail reloaded, the authenticated browser showed
the button, distinct computed success/error styles and a fresh plugin bundle
containing the feedback logic. No real message was opened, copied, sent or
changed. Fictional browser fixtures passed ten reader-copy cases, six signature
cases and 25 compose-Markdown cases; five installation diagnostic tests passed.

## Rollback

On the n0c host, run:

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.6-20260913T141241Z/rollback.py"
```

The script verifies current and backed-up fingerprints before restoring the
three 1.7.5 files. Then follow `tools/web-runtime-maintenance/README.md` to
invalidate Web OPcache for `index.php` in an authenticated session, reload
Mail and verify the served bundle and DOM. Keep the private backup outside
the web root; account, Sieve, calendar and user files are not involved.
