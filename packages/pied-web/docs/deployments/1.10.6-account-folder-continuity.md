# Deployment: account folder continuity 1.10.6

Pied Web **1.10.6** was installed on the n0c production instance on
2026-09-21. Nextcloud remained at **34.0.4**, NextSnapMail at **0.1.14** and
embedded SnappyMail at **2.38.2**. The change is limited to account-menu
navigation in the Pied Web plugin; the theme, core application, settings and
mailboxes were not changed.

## Source and validation

- monorepo commit: `999823a91`;
- release tag: `piedweb-mail-v1.10.6`;
- release archive: `nextsnapmail-pied-web-1.10.6.tar.gz`, SHA-256
  `29ee5b31222bb487ed5a2687f617d36e00f637bbf6f9d6250761111e06b0cc89`;
- the complete monorepo `tools/test.sh` passed, including the reproducible
  63-file payload, 10 Python tests, native account-context checks and all
  mocked mailbox endpoint suites;
- the fictional Feed browser suite passed **24/24**. It covers preserving the
  Sent system-folder intent, resolving a differently named Sent folder in the
  target account and removing the source account's open-message UID.

The source commit and annotated tag were pushed, and the verified archive plus
checksum were published on the GitHub release before production installation.

## Backup and installation

Immediately before installation, the 1.10.5 checker passed all **68** version,
payload, activation and core-patch checks. The exact previous plugin is
recoverable outside the Web root at:

`/home/robindfr/nextsnapmail-pied-web-backups/mail-ux-1.10.5-20260921T122714Z`

The verified archive, checksum, extracted payload, preflight/post-install
reports and guarded deployment script are at:

`/home/robindfr/nextsnapmail-pied-web-staging/1.10.6-20260921T122714Z`

The guarded installer copied only the plugin payload, installed `index.php`
last, and would have restored the saved 1.10.5 plugin on any copy or checker
failure. The 1.10.6 post-install checker passed **68/68**. The unchanged theme
was verified by the same manifest but was not rewritten or backed up again.

## Web runtime and browser checks

LiteSpeed initially continued executing plugin 1.10.5. The authenticated
targeted helper invalidated **17/17** fixed PHP paths; a separate request then
reported **1.10.6** under the LiteSpeed SAPI. Cleanup restored the plugin
configuration byte-for-byte and removed the temporary maintenance plugin. Its
private operator files remain at:

`/home/robindfr/nextsnapmail-pied-web-staging/runtime-maintenance-1.10.6-20260921T122714Z`

A fresh authenticated page loaded the new account-context code with no
temporary helper in its plugin bundle. In a dedicated tab, the read-only check
opened Sent, selected another account, and observed the target account's Sent
entry still selected in native-folder mode, with no foreign message UID, no
pending switch marker and no non-empty error notice. It then returned to the
original account and restored the account Feed. No message was opened, selected
or mutated.

The final installation checker passed **68/68**. The read-only live audit
matched **94/94** tracked files with no mismatches; maintenance remained off,
no database upgrade was pending, and the Calendar patch, WebDAV directives,
scanner short-circuit, HSTS preload, Mail cache and multi-domain image CSP all
remained conforming.

## Rollback

First confirm that production still matches the 1.10.6 manifest. Restore only
`pied-web-ux` from the backup above, copying the saved 1.10.5 `index.php` last.
Run the targeted authenticated Web-runtime invalidation with a fresh helper
suffix, confirm 1.10.5 in a separate request, remove the helper, run the 1.10.5
installation checker and repeat the live audit. Reload Mail only when no
composer or outgoing-delay notice is open. Rollback does not touch messages,
accounts, credentials, folder settings or the unchanged theme.
