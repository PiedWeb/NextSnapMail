# Deployment 1.7.2

**Follow-up:** the web runtime remained on 1.6.4 despite the file installation below.
The [authenticated activation repair](1.7.2-web-runtime-repair.md) diagnoses disabled
OPcache timestamp validation and records actual 1.7.2 browser activation.

Installed 2026-09-12 at 19:12:36 UTC on the configured cloud.piedweb.com installation,
replacing 1.7.1. Nextcloud 34.0.3 / NextSnapMail 0.1.10 / SnappyMail 2.38.2.

All previous fingerprints matched before an external backup. Assets/theme were written
before index.php. Actual runtime compilation passed; all 33 payload files, versions,
activation and the separate linked-account unread core patch matched the read-only audit.
Plugin cache hash: `9f80cfdf9b2ca0999c06105c07fd0a3e`. Theme SHA-256: `7274806e48ad717acb695653ea77e4aa40ff2d11317068847cad54145667ab94`.
Exact fingerprints are in release.json at v1.7.2; [machine record](1.7.2.json).

## What was and was not diagnosed

The owner still saw the old framed 13/4 badge after 1.7.1. Its installed files matched the
release, and its exact server-compiled CSS rendered a borderless, explicit unread label
locally. NextSnapMail was enabled, the old SnappyMail app disabled, and relevant stored
account themes were PiedWeb. The HTTP browser session was unauthenticated (401). Therefore
no specific stale browser/FPM cache or live DOM mismatch was established. Do not equate
file equality or string-presence checks with verified rendering in the owner's browser.

1.7.2 removes the init-class dependency from metadata presentation: CSS now belongs to the
compiled Pied Web theme, uses native DOM/classes and has a quiet numeric fallback without
JS. The metadata script also discovers already-created views. Following gains an amber
star, stronger control surface and row tint, with selection kept teal.

## Verification

15 metadata/native-dispatch cases and 5 theme-only/late-init cases passed. The latter 5 also
passed after deployment using CSS exported from the actual server and transformed with
PageController's embedded-style expression. Five install diagnostics, JS/PHP syntax and
reproducible theme passed. Desktop, 390/320 px and dark snapshots contain fictional mail.
Native click dispatch uses mocked flag updates/navigation. No authenticated live mailbox,
actual flag update, message sending or deletion was used.

## Restore 1.7.1

As the existing installation owner on the server:

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.2-20260912T191236Z/rollback.py"
```

Restores the prior plugin/theme. No files need removing for this release; account, Sieve
and calendar settings are preserved. Finish/cancel pending outgoing mail before switching
versions, then reload. GitHub v1.7.1 separately preserves the previous payload.
