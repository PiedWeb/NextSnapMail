# Deployment: immediate reversible Trash feedback 1.10.4

Pied Web **1.10.4** was installed on the n0c production instance on
2026-09-21. Nextcloud remained at **34.0.4**, NextSnapMail at **0.1.14** and
embedded SnappyMail at **2.38.2**. No core application file, account setting,
message, flag or mailbox content was changed during deployment validation.

## Source and validation

- monorepo commit: `48b504469`;
- release tag: `piedweb-mail-v1.10.4`;
- release archive: `nextsnapmail-pied-web-1.10.4.tar.gz`, SHA-256
  `28fdcd51fc5a5cb5f55cb1a091d90156e8460bfe6e56e01164131326e3f980c0`;
- `tools/test.sh` passed, including the reproducible 63-file payload, native
  account-context suites, 56 account-safe mailbox-operation checks and every
  PHP/Python unit suite;
- targeted fictional browser checks passed selection **23/23** and global
  workspace **20/20**. They hold server replies open to prove that single and
  grouped rows leave the UI before preparation returns, then restore the rows,
  accessibility attributes, focus and selection after a refused operation.

The release was committed and pushed, the annotated tag was pushed, and the
archive plus checksum were published on the GitHub release before installation.

## Backup and installation

The exact 1.10.3 plugin and theme are recoverable outside the Web root at:

`/home/robindfr/nextsnapmail-pied-web-backups/mail-ux-1.10.3-20260921T074238Z`

The verified archive, checksum, extracted payload and deployment script are at:

`/home/robindfr/nextsnapmail-pied-web-staging/1.10.4-20260921T074238Z`

Before any write, the 1.10.3 installation checker matched all payload files,
versions, plugin activation and the optional account-counter patch. The theme
and plugin assets were installed before the version-bearing `index.php`. The
1.10.4 checker then passed every file and version check.

## Web runtime and browser checks

LiteSpeed initially continued executing plugin 1.10.3. The authenticated
targeted helper reported 12 cached paths, invalidated **17 of 17** fixed paths,
and a separate Web request then reported **1.10.4** under the LiteSpeed SAPI.
Cleanup restored `application.ini` byte-for-byte and removed the temporary
maintenance plugin without a concurrent merge.

The authenticated Mail page was reloaded only after confirming that no composer
or delayed send was open. The fresh plugin resource was
`/Plugins/0/User/06fdbb0e5b5c76605e8dc8c7a0389ba6/`. The loaded runtime exposed
`PiedWebUx.mailbox.stageRows`, the native Trash wrapper called the staging path,
and the compiled theme applied `display:none` to the pending state. A reversible
DOM-only probe staged and restored one existing row without reading or changing
its message. The account Feed then settled with 28 rows, no loading state and no
visible error notice.

The final read-only live audit matched **94 of 94** tracked files. Maintenance
remained off with no pending database upgrade; Calendar patches, WebDAV handling,
scanner short-circuit, preload HSTS, the 30-day Mail cache and the multi-domain
image CSP all remained conforming.

## Rollback

First confirm that production still matches the 1.10.4 manifest. Restore only
`pied-web-ux` and `themes/PiedWeb/snappymail` from the backup above, writing the
saved 1.10.3 plugin `index.php` last. Run the targeted authenticated Web-runtime
invalidation, confirm 1.10.3 in a separate request, remove the helper, rerun the
1.10.3 installation checker and the live audit, then reload Mail only when no
composer is open. Rollback does not touch messages, accounts, credentials or
mailbox configuration.
