# Deployment 1.8.4, 2026-09-17

Conversations is now an Inbox-only preference. The control is absent outside
`INBOX`; native list and reader requests are forced to individual-message mode in
Trash, Sent, Drafts, Archive and custom folders. The Pied Web cross-folder reader
stack follows the same boundary. Five plugin files carry the release; the theme is
unchanged.

## Preflight

The production instance reported Nextcloud 34.0.4, NextSnapMail 0.1.11 and embedded
SnappyMail 2.38.2. The installed plugin announced 1.8.3. The four files replaced by
this release matched their 1.8.3 fingerprints, and `inbox-conversations.js` was
absent as expected. No theme or upstream application file was changed.

Full plugin backup outside the web root, before any write:

- `~/nextsnapmail-pied-web-backups/pied-web-ux-1.8.3-20260917T203622Z/`

Its `index.php`, `conversation-thread.js`, `ux.css` and `ux.js` hashes equal the
1.8.3 manifest.

## Installation

The five files were staged privately in
`~/.pw-stage-1.8.4-20260917T203622Z`. Their SHA-256 fingerprints matched the
1.8.4 manifest and the server's PHP accepted `index.php`. Assets were installed
first, then the version-bearing `index.php` last:

- `inbox-conversations.js`
- `conversation-thread.js`
- `ux.css`
- `ux.js`
- `index.php`

The stage was removed after verification. The installed plugin has 39 of 39
manifest files, no missing or foreign file, no fingerprint mismatch, and mode 644
throughout.

Eight LiteSpeed PHP workers were recycled. The first warm-up `status.php` request
answered 200 in 0.73 s and the second in 0.04 s. The authenticated served plugin
bundle key is `379b526b556ee7d69c1f1d25ac6fbdba` and contains the new request guard.

## Verification

- Clean release worktree: `tools/verify.py` reports 43 payload files, reproducible
  theme and passing JS/PHP syntax; five Python diagnostics pass.
- Native PHP conversation checks: 20 endpoint checks and three native header-search
  checks pass, with IMAP transport simulated and no mailbox mutation.
- Browser fixtures: `test-conversation-toggle.js` passes 20 checks; the control is
  hidden in Trash, both POST and cached GET request forms are unthreaded there, and
  Inbox retains the saved preference. `test-virtual-conversation.js` passes 23
  checks, including no conversation lookup for a message opened from Trash.
- Authenticated production DOM: Pied Web is active, the request wrapper is mounted,
  and the Conversations control is visible in Inbox. Navigating to Trash without
  opening a message hides the control with `display:none`; a scoped request has
  `threadUid = 0` and no `useThreads`. Returning to Inbox restores the control.
- The served JavaScript bundle contains the Inbox guard and the inline served CSS
  contains the explicit hidden rule.
- No message was opened, moved, deleted, flagged or marked read/unread during live
  verification.

## Boundaries

The fixture covers both network shapes used by SnappyMail 2.38.2, but no real IMAP
request body was captured because that could expose mailbox data. Production was
verified from the loaded code, the scoped request function and the rendered folder
controls. Mobile and dark mode retain the existing pressed-state checks; the live
folder switch was made in the current desktop light session.

## Rollback

Copy these four files from
`~/nextsnapmail-pied-web-backups/pied-web-ux-1.8.3-20260917T203622Z/` over the
installed plugin and remove the new file:

- restore `conversation-thread.js`, `ux.css`, `ux.js` and `index.php`;
- remove only
  `~/nextcloud/data/appdata_nextsnapmail/_data_/_default_/plugins/pied-web-ux/inbox-conversations.js`.

Then recycle the account's `lsphp` workers, warm `status.php`, and confirm the served
plugin bundle key returns to the 1.8.3 build. The full backup can restore the whole
plugin directory if needed. This release writes no mailbox data or account setting;
the saved `UseThreads` preference is unchanged.
