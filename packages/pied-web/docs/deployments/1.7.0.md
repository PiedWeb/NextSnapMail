# Deployment 1.7.0

Installed 2026-09-12 at 18:35:27 UTC on the existing Nextcloud host, replacing 1.6.6.
Compatibility: Nextcloud 34.0.3, NextSnapMail 0.1.10, SnappyMail 2.38.2.

All previous plugin/theme fingerprints matched before deployment. An external backup was
created before writing assets; index.php was installed last. Actual Nextcloud/SnappyMail
JS/CSS compilation and unread-drafts endpoint registration passed. The read-only diagnostic
matched all 32 payload files, versions, activation and the existing linked-account core patch.
Plugin cache hash: `e875f0f03f0fd4485d6ba81ddc0854cb`. Machine-readable record: [1.7.0.json](1.7.0.json).
Exact payload hashes: `release.json` in tag `v1.7.0`.

## Restore 1.6.6

Run as the existing installation owner on the host:

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.0-20260912T183527Z/rollback.py"
```

This restores the previous plugin/theme and removes UnreadDrafts.php and unread-drafts.js.
Finish or cancel pending outgoing messages before switching versions; reload the webmail.
Account, Sieve and calendar settings are preserved. The GitHub v1.6.6 release also contains
that payload independently of this host backup.

## Verification boundary

25 browser draft scenarios and 19 PHP endpoint/parser checks cover actual unread filtering,
UID collisions, pagination, empty Inbox, native draft metadata, retries and account changes.
22 image/editor regressions, 25 filtered-selection checks, 10 native attachment storage checks,
5 installation diagnostics and payload syntax/reproducible-build checks passed.

Draft browser tests use actual native MessageCollectionModel, MessageModel, EmailModel and
attachment models extracted from the referenced engine, but mock transport, popup opening
and the HTML rendering helper. No authenticated live mailbox session, mail sending, deletion
or flag mutation was used. Actual installed compilation/fingerprints were verified via SSH.
Desktop, 390 px mobile, 320 px and OS dark-mode screenshots use fictional mail only.

See [Unread drafts](../UNREAD_DRAFTS.md) for first-page/search scope, refresh rules and
normal native saving behavior. Draft reminders deliberately remain outside Inbox checkbox
selection because native selection and delete commands assume one source folder.
