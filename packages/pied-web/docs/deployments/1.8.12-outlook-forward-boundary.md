# Deployment 1.8.12: Outlook forwarding boundary, 2026-09-18

Outlook forwarding now keeps the author's current note and signature visible, then folds the
forwarded message from its real four-field mail header. A broad `dir="ltr"` mail wrapper can no
longer inherit labels and line breaks from nested history and become the all-or-nothing fold.

## Preflight and backup

Production reported plugin 1.8.11. The two target files matched release 1.8.11 before the
write:

- `quote-readability.js`: `429e5fc85a0da810572f59d62f321628b3779c7230c6ddd2e9c44ad2fb34efb6`
- `index.php`: `1656682f857b009a709a88ec6037e97ee59a02ad0433482caf713504b4b82448`

The complete plugin was backed up outside the web root at:

`~/nextsnapmail-pied-web-backups/pied-web-ux-1.8.11-20260918T090543Z/`

## Installation

Only `quote-readability.js` and the version-bearing `index.php` were installed, in that
order. Their production SHA-256 values are:

- `quote-readability.js`: `313ece139d33c6b9f786937f7f073625db36748bb3a1cb3ed9e7ef7c22ed1f66`
- `index.php`: `0c7b1380cdeab7d3ae2d185c95b358ee30a18f4f2071daa34269386b2a98b186`

Server PHP syntax passed. The authenticated runtime check first observed stale 1.8.11 code,
then invalidated the fixed plugin PHP paths and confirmed 1.8.12 in a fresh LiteSpeed request.
The temporary maintenance plugin was removed, its private configuration backup was restored
byte-for-byte and the private staging directory was deleted after verification.

## Verification

- Package verification passed for all 51 runtime files, the reproducible theme and
  JavaScript/PHP syntax; five Python diagnostics passed.
- The fictional native-reader browser fixture passed 12 checks. The new case places a ruled
  Outlook header inside a broad `dir="ltr"` wrapper and proves that the current note stays
  outside the native disclosure.
- The post-deployment read-only audit matched all 56 version, payload, activation and optional
  patch checks for release 1.8.12.
- The owner explicitly authorized opening the production conversation
  `TR: Séjours créateur de contenu - Vercors - mai 2026` on `robin@piedweb.com`. Before the
  write, a read-only DOM simulation selected the inner four-field header rather than the
  wrapper, leaving 499 text characters before the boundary. After deployment and reload, the
  real reader showed the current note and signature, followed by one closed native
  **Afficher la citation** disclosure after the horizontal rule. The disclosure contained the
  forwarded history and its six nested native quote nodes.
- The served plugin bundle
  `/apps/nextsnapmail/?/Plugins/0/User/d89ebcbba1759717d9796e2d0d0140d7/`
  contained the scoped-label and horizontal-rule boundary code.
- The requested message was already read. It was not moved, flagged, deleted, replied to or
  forwarded, and no message content was copied into the repository or fictional fixture.

## Rollback

Restore `quote-readability.js` and `index.php` from
`~/nextsnapmail-pied-web-backups/pied-web-ux-1.8.11-20260918T090543Z/`, invalidate the restored
`index.php` through the authenticated runtime-maintenance procedure, remove that temporary
helper, reload Mail and confirm version 1.8.11 and the previous plugin bundle. Mail data and
account settings are not part of this change.
