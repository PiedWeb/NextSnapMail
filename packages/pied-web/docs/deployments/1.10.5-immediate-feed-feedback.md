# Deployment: immediate Feed feedback 1.10.5

Pied Web **1.10.5** was installed on the n0c production instance on
2026-09-21. Nextcloud remained at **34.0.4**, NextSnapMail at **0.1.14** and
embedded SnappyMail at **2.38.2**. No message, flag, reminder, account setting
or mailbox content was changed during deployment validation.

## Source and validation

- monorepo commit: `4df138663`;
- release tag: `piedweb-mail-v1.10.5`;
- release archive: `nextsnapmail-pied-web-1.10.5.tar.gz`, SHA-256
  `61b68e5b7ea44887aba2678f3e0d18d9d0e9fd7b6b7b43b036d55fa82afc7892`;
- the complete monorepo `tools/test.sh` passed, including the reproducible
  63-file payload, 10 Python unit tests, native endpoint suites and all 56
  account-safe mailbox-operation checks;
- delayed fictional browser checks passed global workspace **29/29**, native
  selection **23/23** and reminders **16/16**. They cover optimistic Flag and
  read state, exact rollback, reminder row staging, stale-search isolation,
  explicit busy controls, keyboard behavior and responsive geometry.

The source commit and annotated tag were pushed, and the verified archive plus
checksum were published on the GitHub release before production installation.

## Backup and installation

The exact 1.10.4 plugin and theme are recoverable outside the Web root at:

`/home/robindfr/nextsnapmail-pied-web-backups/mail-ux-1.10.4-20260921T111146Z`

The verified archive, checksum, extracted payload, preflight/post-install
reports and guarded deployment script are at:

`/home/robindfr/nextsnapmail-pied-web-staging/1.10.5-20260921T111146Z`

Immediately before copying, the 1.10.4 checker passed all **68** version,
payload, activation and core-patch checks. The guarded installer then backed up
both components, installed plugin and theme assets before the version-bearing
`index.php`, and passed the 1.10.5 checker **68/68**. A failed copy or checker
would have restored the saved 1.10.4 assets with its index installed last.

## Web runtime and browser checks

LiteSpeed initially continued executing plugin 1.10.4. The authenticated
targeted helper invalidated **17 of 17** fixed PHP paths, and a separate Web
request then reported **1.10.5** under the LiteSpeed SAPI. Cleanup restored the
plugin configuration byte-for-byte and removed the temporary maintenance
plugin without a concurrent merge. Its private files remain at:

`/home/robindfr/nextsnapmail-pied-web-staging/runtime-maintenance-1.10.5-20260921T111146Z`

Before reloading Mail, the authenticated tab had no composer, outgoing-delay
notice or schedule panel open. The fresh plugin resource was
`/Plugins/0/User/a056b6839e9935a4ee20c2822c1389e8/`. The loaded runtime exposed
the new optimistic Flag and bulk-read paths, stale-search isolation and the
general row-staging primitive; the compiled theme supplied the pending-row,
stale-result and disabled-control rules. A synthetic DOM-only probe staged and
restored a temporary row and measured the disabled row-action cursor/opacity.
It did not inspect or mutate a real message. The account Feed then settled with
27 native rows, no loading state and no visible error notice.

The final read-only live audit matched **94 of 94** tracked files. Maintenance
remained off with no pending database upgrade; Calendar patches, WebDAV
handling, scanner short-circuit, preload HSTS, the 30-day Mail cache and the
multi-domain image CSP all remained conforming.

## Rollback

First confirm that production still matches the 1.10.5 manifest. Restore only
`pied-web-ux` and `themes/PiedWeb/snappymail` from the backup above, writing the
saved 1.10.4 plugin `index.php` last. Run the targeted authenticated Web-runtime
invalidation, confirm 1.10.4 in a separate request, remove the helper, rerun the
1.10.4 installation checker and the live audit, then reload Mail only when no
composer or outgoing-delay notice is open. Rollback does not touch messages,
accounts, credentials, reminders or mailbox configuration.
