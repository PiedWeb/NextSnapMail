# Deployment 1.8.13: active message joins Ctrl-click selection, 2026-09-18

Starting desktop multi-selection from the reading pane now carries the open message into the
checked group before adding the Ctrl/Command-clicked row. The previous capture handler toggled
only the clicked row and skipped the equivalent native SnappyMail step.

## Preflight and backup

Production reported Nextcloud 34.0.4.1 and plugin 1.8.12. Both target files matched release
1.8.12 before the write:

- `list-interactions.js`: `d0c6981b421ff99961f0edf229a4eb170921d0ef964b57da9171aca5e1919a00`
- `index.php`: `0c7b1380cdeab7d3ae2d185c95b358ee30a18f4f2071daa34269386b2a98b186`

The complete plugin was backed up outside the web root at:

`~/nextsnapmail-pied-web-backups/pied-web-ux-1.8.12-20260918T091337Z/`

## Installation

Only `list-interactions.js` and the version-bearing `index.php` were installed, in that order.
Their production SHA-256 values are:

- `list-interactions.js`: `61bbfde9c016e21ba3fb6328f28f558ce75b762c268673745ba2a0a603424dd0`
- `index.php`: `01b83dbb0ec34e9b4c445aadf9b1b0a15edfad10895c341ca4676532da8fc426`

Server PHP syntax passed. The authenticated runtime check first observed stale 1.8.12 code,
invalidated only the fixed plugin PHP paths, then confirmed 1.8.13 in a fresh LiteSpeed request.
The temporary maintenance plugin was removed, its private configuration backup was restored
byte-for-byte and the private staging directory was deleted after verification.

## Verification

- Package verification passed for all 51 runtime files, the reproducible theme and JavaScript/
  PHP syntax; five Python diagnostics passed.
- The fictional selection fixture passed 18 browser scenarios. Its new regression opens the
  third row, Ctrl-clicks the first and verifies that both are checked, the banner says two,
  the clicked row does not open and later grouped commands keep their existing targets.
- The neighboring metadata fixture passed all 15 functional assertions. Its final screenshot
  step timed out after fonts loaded; no metadata assertion or runtime behavior failed.
- The post-deployment read-only audit matched all 56 version, payload, activation and optional
  core-patch checks for release 1.8.13.
- The freshly served plugin bundle contained the active-message carry code. A reversible live
  browser check started from an open or already-read row, Ctrl-clicked another row and observed
  both the active and clicked models in the checked group, with a count of two. The check then
  used **Done** and restored the active row. It did not read or copy message content, change a
  read flag, move, flag, delete, reply to or forward a message.

## Rollback

Restore `list-interactions.js` and `index.php` from
`~/nextsnapmail-pied-web-backups/pied-web-ux-1.8.12-20260918T091337Z/`, invalidate the restored
`index.php` through the authenticated runtime-maintenance procedure, remove that temporary
helper, reload Mail and confirm version 1.8.12 and the previous plugin bundle. Mail data and
account settings are not part of this change.
