# Deployment 1.7.12, 2026-09-14

The first Pied Web Confort proposal increases desktop breathing room from 1200 px upward: a 248 px folder pane, a message list growing toward 540 px, taller rows, and a narrower reading measure. The mobile and medium-width theme rules are unchanged. The 1.7.10 virtual Sent conversation module remains installed.

Before installation, Nextcloud 34.0.4, NextSnapMail 0.1.11 and SnappyMail 2.38.2 matched the 35-file 1.7.10 manifest. Only `theme/PiedWeb/snappymail/style.css` and `plugin/pied-web-ux/index.php` changed. The first 1.7.11 staging pass exposed SnappyMail's inline pane widths and its compiled CSS cache; it was corrected before publication. The final 1.7.12 stylesheet SHA-256 is `52ce6027b1d68b7aa7a1591446b5ed632be636f9cca361177a8f738c717a836a`, and the plugin index SHA-256 is `fe7b60c96468a9fa75bc87ee2e86478ecaddfaf1d223a37a8f785b12e1feb78c`.

The deployment stages and guarded scripts are outside the web root at `~/nextsnapmail-staging/pied-web-ux-1.7.11-20260914T115750Z/` and `~/nextsnapmail-staging/pied-web-ux-1.7.12-20260914T120900Z/`. The first-pass CSS is retained in the 1.7.11 stage for diagnosis. A complete 1.7.10 rollback of the two changed files is prepared at `~/nextsnapmail-backups/pied-web-ux-v1.7.12-20260914T120900Z/`. Every installed file was fingerprint-checked against the previous manifest before replacement and against the final manifest afterward.

The authenticated LiteSpeed Web runtime initially reported 1.7.10, then 1.7.11 while the final cache key was being prepared. Each changed plugin index was invalidated through the targeted native administrator hook, and distinct follow-up requests confirmed 1.7.11 and finally 1.7.12. All temporary maintenance plugins were removed; each configuration backup matched the restored `application.ini` byte-for-byte. Production CLI compilation reported 1.7.12, plugin hash `be37d79bc717d91457f3bd7bcc0da7af`, and the final CSS rule. The public CSS URL served the exact final SHA-256.

After cleanup, a fresh authenticated Chrome page loaded the final compiled CSS and plugin bundle. At a 1920 px viewport, the folder pane measured 248 px, the list 540 px, and a message row 72 px. The bundle retained the virtual Sent conversation module and contained no maintenance hook. No private messages were captured or opened for this check. The 390 px fictional fixture was pixel-identical before and after the desktop change; 320 px had no horizontal overflow. The five Python tests, reproducible-theme and 35-file manifest verification, PHP/JS syntax checks, and 60 browser fixture checks passed.

## Rollback

On the n0c host, run:

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.12-20260914T120900Z/rollback.py"
```

The script requires all 35 installed files to match 1.7.12, verifies the saved 1.7.10 index and stylesheet, then restores those two files. Follow `tools/web-runtime-maintenance/README.md` to invalidate the restored PHP class in authenticated LiteSpeed Web runtime and reload Mail. The rollback does not alter messages, accounts or the 1.7.10 virtual Sent module.
