# Deployment 1.7.3

Files installed on 2026-09-12 at 20:02:25 UTC, replacing 1.7.2, on the n0c
installation used by `https://nc.robin-d.fr/apps/nextsnapmail/`.
Nextcloud 34.0.3 / NextSnapMail 0.1.10 / SnappyMail 2.38.2.

All prior file fingerprints matched before the backup. Assets/theme were installed
before index.php. The native CLI compilation checks passed; 34 installed payload
files, activation, versions and the linked-account unread core patch passed the
read-only audit (39 checks). No account, Sieve or calendar settings changed.

CLI plugin hash: `519488cb17ad6c667ac699e0dd78e137`.
Theme SHA-256: `2f810729cb69c1e31143e40b28e966c37a0f3e278bcd54ac30089be52c08d691`.
Exact file fingerprints are in release.json.

## Web activation status: verified, 2026-09-12 20:45 UTC

The owner's follow-up confirmed the surrounding header was still visible. The loaded
bundle was `9e35e4c379f4c903fc91fda40528b7c4`, and the authenticated maintenance hook
measured **web PHP 1.7.2**, despite the installed 1.7.3 files. LiteSpeed OPcache still
had `validate_timestamps=0`.

Targeted invalidation of the four fixed plugin PHP paths activated **1.7.3**, confirmed
by a subsequent authenticated request. The temporary helper was removed and configuration
restored byte-for-byte. Private maintenance files remain outside the web root under
`nextsnapmail-staging/pied-web-runtime-173-final-20260912/`.

After reload with no composer open, the actual web bundle is
`5b0908a45dfa8c4d420fb0d4ba9b2a7c`. Its response contains the mail-shell implementation.
Both root and body have `pw-mail-shell`; the theme marker reports 1.7.3. On desktop,
the header box is 0 × 0 and mail fills x=0/y=0, 1832 × 996. The original launcher is
44 × 44 at x=8/y=6 and opens its native 15 app links. At 390 × 844 the mail fills the
viewport without horizontal overflow; the single mobile header is 64 px high, the
launcher is at x=8/y=10 and the identity starts at x=60. Desktop and mobile header
screenshots were inspected and remain private, outside Git. The separate mobile test
tab was closed and the owner's corrected desktop tab left open.

All 39 installation checks pass, including all 34 payload files. No mail was opened,
sent, deleted, flagged or marked read/unread. No runtime source changed in this repair;
the existing v1.7.3 tag and archive are retained unchanged.

## Initial rollout history

The current changes were previewed in the authenticated desktop session before it
became unavailable. In that preview, the mail started at x=0/y=0, both checked boxes
shared the star's vertical center, and the original app menu opened with its actual
15 application links. Two existing checkboxes were checked and cleared; no mail was
opened, sent, deleted or flagged. Private screenshots are excluded from this repository.

The Chrome connection initially timed out. Following the user's startup rule, work moved
to the standalone browser, where there was no authenticated mail tab. A request to
make the connected Chrome session available was pending. The initial file installation
did not certify web activation. The authenticated follow-up above completes that rollout.

## Validation

18 fixture cases cover desktop, 390/320 px, dark mode, compact rail, theme switching,
browser-history restoration, missing-launcher fallback and same-origin iframe.
15 native metadata/dispatch cases pass with fictional mail and mocked transport.
PHP/JS syntax, reproducible theme, five installation-diagnostic unit tests and the
post-install fingerprint audit pass. No delivery or destructive mailbox test was used.

## Restore 1.7.2

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.3-20260912T200225Z/rollback.py"
```

The rollback restores the earlier plugin/theme and removes the newly added app-shell.js.
It also requires targeted **web OPcache invalidation**, followed by web verification.
The backup is outside the web root; account/Sieve/calendar configuration is preserved.
