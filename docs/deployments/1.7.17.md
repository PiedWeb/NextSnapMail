# Deployment 1.7.17, 2026-09-14

Desktop day headings at 1200 px and wider now show a short rule before the label and the existing long rule after it, without moving the label. The previous 1.7.16 removal of the row divider before a date remains. A fictional 390 px mobile capture is pixel-identical to 1.7.16.

Before installation, Nextcloud 34.0.4, NextSnapMail 0.1.11 and embedded SnappyMail 2.38.2 matched all 35 files in the 1.7.16 manifest. The guarded deployment changed only `theme/PiedWeb/snappymail/style.css` (SHA-256 `4ebc297768f805faa5276fb5b8bbc006d36beabc3cba83c7975ab10fd16cbf10`) and version-bearing `plugin/pied-web-ux/index.php` (SHA-256 `5155bd498d522ef9dee8bc4998b1bad8d48633023f45a47c1925b4be28af1110`). CSS was installed before PHP, and all 35 new hashes matched. The private staging directory is `~/nextsnapmail-staging/pied-web-ux-1.7.17-20260914T151343Z/`; the exact previous files, both manifests and a fingerprint-guarded rollback script are at `~/nextsnapmail-backups/pied-web-ux-v1.7.17-20260914T151343Z/`, outside the web root. The public stylesheet served the new SHA-256.

The authenticated Chrome session was reached through the migrated agent profile. The temporary runtime hook initially reported **1.7.15**, confirming that the prior 1.7.16 file install had not advanced the LiteSpeed PHP class. The hook invalidated its four fixed Pied Web PHP files; a separate web request then reported **1.7.17**. Cleanup restored `application.ini` byte-for-byte and removed the temporary plugin. After reloading Mail, the served plugin bundle (SHA-256 `1e3512b9985345396ab323435bf43777d1da91e24799c109c51868790d02b47f`) retained the selection and Sent conversation code and contained no temporary hook.

Read-only checks in the live 1920 px Mail list found 20 rows and five day headings: their leading rule was 20 px, the label retained 16 px start padding, the trailing rule still flexed, the row border before a heading was transparent, and row times remained hidden. No production message content was captured or state changed. Fictional desktop browser checks passed 13 scenarios; selection checks, five Python tests, syntax and reproducible theme/manifest checks also passed. The 390 px fictional mobile screenshot had no pixel differences from 1.7.16. See `docs/previews/1.7.17/`.

## Rollback

On the n0c host, run:

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.17-20260914T151343Z/rollback.py"
```

The script requires all 35 installed files to match 1.7.17 and verifies the saved 1.7.16 files before restoring only the two changed files. Then follow `tools/web-runtime-maintenance/README.md` to invalidate the restored PHP class in the authenticated LiteSpeed web runtime and reload Mail. No account, message or calendar data is involved.
