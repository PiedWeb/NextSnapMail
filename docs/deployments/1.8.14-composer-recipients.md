# Deployment 1.8.14: Cc and Cci beside To, 2026-09-18

The optional recipient controls now sit immediately below the **To** input, aligned to the
end of its input column as quiet grey text. Each shortcut disappears when its native field is
open and returns when that field is closed. The original header links remain available outside
the Pied Web theme, and the native advanced-fields menu is unchanged.

## Preflight and backup

The read-only production check matched release 1.8.13 across Nextcloud 34.0.4,
NextSnapMail 0.1.11, SnappyMail 2.38.2, all 51 payload files, plugin activation and the
optional unread-count patch. There was no drift before the write.

The complete plugin and theme were backed up outside the web root at:

- `~/nextsnapmail-pied-web-backups/pied-web-ux-1.8.13-20260918T103717Z/`
- `~/nextsnapmail-pied-web-backups/PiedWeb-theme-1.8.13-20260918T103717Z/`

The rollback copies retain these release 1.8.13 hashes:

- `ux.js`: `1f517d4eb617c8054f23b58ee12cde41f1623e2dbecb8bac7836f9a2d6b14022`
- `style.css`: `87ae870acbc6273512bdc59111c8eda055154abe592077bcce8a0948826ca101`
- `index.php`: `01b83dbb0ec34e9b4c445aadf9b1b0a15edfad10895c341ca4676532da8fc426`

## Installation

Only three files changed. They were staged privately, compared with the release manifest and
accepted by server PHP syntax before installation. Assets were installed before the
version-bearing PHP file:

1. `plugin/pied-web-ux/ux.js`
2. `theme/PiedWeb/snappymail/style.css`
3. `plugin/pied-web-ux/index.php`

Their installed SHA-256 values are:

- `ux.js`: `275a1442cc1c9894044aa3d1a269b68113e49321548ddf96cb8c1ddddb84ca63`
- `style.css`: `131098e5901a78034d7754c16d232672e71d4de46974fb4820fb0c489ed6fde0`
- `index.php`: `d0028965e9dcf32a7c3d6ada5c69879f3c24689bb71d706c94ff76af1d4d6952`

The authenticated runtime hook first observed stale 1.8.13 code, invalidated only the fixed
Pied Web PHP paths, then confirmed 1.8.14 from a fresh LiteSpeed request. Cleanup restored the
private configuration byte-for-byte and removed the temporary maintenance plugin and staging
directory.

## Verification

- Package verification passed for all 51 runtime files, the reproducible theme, JavaScript/PHP
  syntax and five Python diagnostics.
- The new fictional native-composer fixture passed 10 scenarios: placement below To, right
  alignment, hidden header duplicates, quiet resting style, hover underline, per-field
  visibility, focus transfer, keyboard ring, narrow layout and theme restoration.
- Neighboring fixtures passed 7 composer-action, 10 inline-reply, 19 scheduled-send and 6
  native-control checks.
- The post-deployment installation check matched all release 1.8.14 payload/version checks.
  The parent read-only audit matched all 78 tracked files and retained the Calendar patch,
  `.htaccess` directives and Mail attachment cache window.
- The freshly served plugin bundle key is `75806b31a832681e01c235398dc22964`; its response contains
  the recipient-shortcut code. The freshly compiled Pied Web theme contains the matching rules.
- A blank production composer showed **Cc | Cci** below To, with both header duplicates hidden.
  Choosing Cc revealed and focused the native Cc input, hid only the Cc shortcut and kept Cci
  available. The blank composer was then explicitly discarded. No recipient, subject or body
  was entered, and no message was saved or sent.

## Rollback

Restore `ux.js` and `index.php` from the plugin backup and `snappymail/style.css` from the theme
backup above, installing `index.php` last. Invalidate the restored PHP file through the
authenticated runtime-maintenance procedure, remove that temporary helper, reload Mail and
confirm release 1.8.13 and its previous bundle. Mail data, accounts and settings are not part
of this change.
