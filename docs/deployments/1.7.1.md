# Deployment 1.7.1

Installed 2026-09-12 at 18:49:24 UTC, replacing 1.7.0 on the existing host.
Nextcloud 34.0.3 / NextSnapMail 0.1.10 / SnappyMail 2.38.2.

All prior plugin/theme hashes matched. External backup preceded installation; assets were
written before index.php. Actual runtime JS/CSS compilation passed, including metadata UI
and styles. All 33 installed files, versions, activation and the separate account-unread
core patch matched the read-only installation diagnostic.

Plugin cache hash: `689ca272d22a64b1ffbcb886e941b30a`. Exact payload hashes are in release.json at v1.7.1.
Machine-readable record: [1.7.1.json](1.7.1.json).

## Restore the previous version

As the existing installation owner on the server:

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.1-20260912T184924Z/rollback.py"
```

Restores 1.7.0 and removes list-metadata.js. Account, Sieve and calendar settings are preserved.
Finish/cancel pending outgoing mail before changing versions, then reload the webmail.
GitHub release v1.7.0 independently preserves the same prior payload.

## Checks and limits

15 metadata browser checks use the extracted native list click dispatcher with mocked flag
updates/navigation. They cover mouse, Space/Enter, native multi-selection behavior, live
unread counts, hidden counts, French/English, theme exit and desktop/mobile/dark geometry.
25 unread-draft regressions also passed, plus 5 install diagnostics, syntax and reproducible
build. No authenticated live browser session or actual flag/mail mutations were used.
The actual server compilation and file comparisons were verified over SSH.

Fictional before/after, 390 px, 320 px and dark screenshots are in docs/screenshots.
At 320 px a CSS-specificity collision initially prevented the metadata reflow; it was fixed
and an explicit sender-width/metadata-position assertion now covers that case.
See [List metadata](../LIST_METADATA.md) for the preserved native contracts.
