# Deployment 1.7.22, 2026-09-14

The owner asked for the conversation heading to show the original subject,
while retaining the open message's own subject at sender-line size. Version
1.7.22 uses the earliest chronological message subject as the heading and
hides it during initial lookup. The later message subject remains in SnappyMail's
native reader, at 14 px. No mailbox content or account setting was changed.

Immediately before installation, all 35 live 1.7.21 payload hashes matched.
The guarded deployment saved the exact previous `conversation-thread.js`,
compiled theme `style.css` and version-bearing `index.php` outside the web root,
then installed only those three files, with PHP last. All 35 new hashes matched.
The private stage is
`~/nextsnapmail-staging/pied-web-ux-1.7.22-20260914T170345Z/`; the exact
previous files, both manifests and fingerprint-guarded rollback script are in
`~/nextsnapmail-backups/pied-web-ux-v1.7.22-20260914T170345Z/`.

The public stylesheet served SHA-256
`80bb4820574e1403bda3d087f95ebb1d50f024d7da055c293008ef2b1555b885`.
Production CLI compilation reported version 1.7.22, plugin hash
`343f650b78ced99b0ab513a890d1dd18`, the conversation module and native
message selection present, and unchanged configuration. The read-only audit
found 52/52 tracked hashes matching, Nextcloud 34.0.4 installed and maintenance
mode off; see `1.7.22.json`.

Nineteen fictional browser checks covered earliest-subject selection, stable
heading while switching messages, native subject typography, delayed lookup,
read-only conversation assembly, desktop/mobile actions, retry and account
isolation. Thirteen native label-menu browser checks, three native header-search
parser checks, five Python diagnostics and reproducible release verification
passed. No production message was sent, copied, moved or deleted.

The owner's authenticated Mail request invalidated the targeted Web PHP class;
a subsequent request reported `version:1.7.22`, `invalidated:true` and
`error:false`. The temporary administrator-only maintenance hook installed for
1.7.21 was then removed, with its exact configuration backup restored. Final
CLI compilation still reported 1.7.22, the conversation module and native
selection present, unchanged configuration, and plugin hash
`2e5fddff5f0833530388b7d2eed7a928`. The public stylesheet still served the
new fingerprint, and the final read-only audit matched 52/52 tracked files.
The owner has been asked to reload Mail and confirm the changed title and
typography in the authenticated reader; that final DOM check is pending.

## Rollback

On the n0c host, run:

```sh
python3 "$HOME/nextsnapmail-backups/pied-web-ux-v1.7.22-20260914T170345Z/deploy.py" rollback
```

The script verifies all current 1.7.22 payload hashes and saved 1.7.21 files
before restoring only the three changed files. Then follow
`tools/web-runtime-maintenance/README.md` to invalidate the restored PHP class
in the authenticated Web runtime and reload Mail.
